/*
js/sidepanel.js: Este é o script que controla o comportamento do seu painel lateral. Ele é executado no contexto do painel lateral e tem acesso a um subconjunto das APIs do Chrome. Ele é usado para manipular o DOM do painel lateral, responder a eventos do usuário no painel lateral, e se comunicar com o script de background se necessário.
 */